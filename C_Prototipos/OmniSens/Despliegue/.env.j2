# Entorno PostgreSQL
POSTGRES_USER={{ vault_postgres_user | default('postgres') }}
POSTGRES_PASSWORD={{ vault_postgres_password | default('str0ngP4ssw0rd') }}
POSTGRES_DB={{ vault_postgres_db | default('aqi_db') }}

# Entorno MQTT (Credenciales Backend)
MQTT_BACKEND_USER={{ vault_mqtt_backend_user | default('backend_service') }}
MQTT_BACKEND_PASS={{ vault_mqtt_backend_pass | default('b4ckend_s3cr3t') }}

# JWT Secret para el Backend Fastify
JWT_SECRET={{ vault_jwt_secret | default('super_secret_jwt_key_omnisens_2026') }}
